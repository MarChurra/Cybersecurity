# Level Goal

The password for the next level is stored in the file data.txt, which contains base64 encoded data.

The goal was to first pass the file into the Base64 decoder and then convert into a ASCII string the output.

## Technologies used:
    -Powershell
    -SSH
    -Queried Search with Flags
    -Strings
    -Base64
