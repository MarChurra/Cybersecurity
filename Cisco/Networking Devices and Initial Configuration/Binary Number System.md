## Binary
- IPv4 human-readable format uses a base of 10 and the powers of 0, 1 ,2, and so on.
- Binary uses Base 2, to power of 0, 1, 2 and so on.

- FOr IPv6 you muyst be able to convert hexadecimal to decimal and vice versa.
- Hexadecimal is a base sixteen system, using numbers from 0 to 9 and letters A to F.
- IPv6 address are 128 bits in legths and every 4 bits represent a single hexadecimal digit, for a total of 32 hexadecimal values.
- THese addresses are not case sensitive.
- Hextet is the unofficial term used to refer to a segment of 16 bits or four hexadecimal values.
